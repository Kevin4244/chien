<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Cani-Code!{% endblock %}</title>
    <link rel="icon" href="{{ asset ('img/img_1.png') }}">
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset("styles/dog.css") }}">
    {% endblock %}

    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body class="d-flex flex-column min-vh-100">
<header>
    <nav class="navbar navbar-expand-lg mb-5">
        <div class="container-fluid">
            <a class="navbar-brand text-light" href="{{ path('app_bucket') }}">Bucket-List</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active text-light" aria-current="page" href="{{ path('app_home') }}">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-light" href="{{ path('app_about') }}">About us</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-light" href="{{ path('app_list') }}">List</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-light" href="{{ path('app_details') }}">Details</a>
                    </li>


                </ul>
                <form class="d-flex" role="search">
                    <input class="form-control me-2 border border-light" type="search" placeholder="Search"
                           aria-label="Search">
                    <button class="btn border border-light text-light" type="submit">Search</button>
                </form>
            </div>
        </div>
    </nav>
</header>

<div class="flex-fill">
    {% block body %}{% endblock %}
</div>
<div class="dog"></div>
<footer class="d-flex justify-content-center text-light">
    <div>
        <p>
            © Bucket-List {{ "now"|date("Y") }}. All rights reserved.
        </p>
    </div>
</footer>
</body>
</html>
